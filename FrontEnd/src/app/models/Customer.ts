export class Customer {
  name: string;
  address: string;
  salary: number;

  constructor(name: string, address: string, salary: number) {
    this.name = name;
    this.address = address;
    this.salary = salary;
  }
}
